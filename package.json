{
  "name": "ride-local-impact-global",
  "version": "1.0.0",
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "dev": "vite",
    "my-app:dev": "cd my-app && npm install && npm run dev",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.2.6"
  }
}
